export default {
  clearMocks: true,
  testEnvironment: 'node',
  collectCoverage: true,
  coverageDirectory: 'coverage',
  coverageProvider: 'v8',
  testMatch: ['**/**/*.test.js'],
  verbose: true,
  forceExit: true,
};
